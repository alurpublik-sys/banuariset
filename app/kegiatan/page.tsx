"use client";
import {useEffect,useState} from "react";
import {supabase} from "../../lib/supabase";
import "../globals.css";
export default function ActivitiesPage(){const[rows,setRows]=useState<any[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{supabase.from('events').select('id,title,slug,category,excerpt,cover_image_url,event_date,location').eq('status','published').order('event_date',{ascending:false}).then(({data})=>{setRows(data??[]);setLoading(false)})},[]);return <main><header className="header"><div className="head shell"><a href="/" className="brand"><img src="https://raw.githubusercontent.com/alurpublik-sys/banuariset/main/public/banua-logo.png" alt="Banua Research"/></a><a href="/">← Beranda</a></div><div className="orange"/></header><section className="section shell"><div className="title"><h2>Kegiatan</h2><i/></div>{loading?<p>Memuat kegiatan...</p>:rows.length===0?<p>Belum ada kegiatan yang diterbitkan.</p>:<div className="cards">{rows.map(r=><a className="card" href={`/kegiatan/${r.slug}`} key={r.id}><span>{r.category||'KEGIATAN'}</span><h3>{r.title}</h3><p>{r.excerpt||[r.location,r.event_date?new Date(r.event_date).toLocaleDateString('id-ID'):null].filter(Boolean).join(' · ')}</p><b>→</b></a>)}</div>}</section></main>}
